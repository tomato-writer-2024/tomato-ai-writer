# 番茄小说AI写作工具 - 部署指南

> **详细的部署步骤，让你的工具上线运行**

---

## 📋 部署前准备

在开始部署之前，请确保已完成以下配置：

- [ ] 已获取豆包API Key
- [ ] 已完成环境变量配置
- [ ] 已完成数据库迁移
- [ ] 已完成本地功能测试

---

## 部署平台选择

推荐以下部署平台：

| 平台 | 费用 | 推荐度 | 适合场景 |
|------|------|--------|---------|
| Vercel | 免费 | ⭐⭐⭐⭐⭐ | Next.js项目首选 |
| Netlify | 免费 | ⭐⭐⭐⭐⭐ | 简单易用 |
| Railway | 有免费额度 | ⭐⭐⭐⭐ | 全栈应用 |
| Render | 有免费额度 | ⭐⭐⭐⭐ | 需要数据库 |
| 阿里云/腾讯云 | 付费 | ⭐⭐⭐ | 国内访问快 |

---

## 方案一：使用Vercel部署（推荐）

### 优势

✅ 完全免费（个人项目）
✅ 自动部署，提交代码自动更新
✅ 全球CDN加速
✅ HTTPS自动配置
✅ 自定义域名支持

### 步骤1：注册Vercel账号

1. 访问 https://vercel.com/
2. 点击右上角 "Sign Up"
3. 使用GitHub、GitLab或Bitbucket账号注册
4. 选择个人计划（Free）

### 步骤2：准备代码仓库

**如果没有GitHub账号：**

1. 访问 https://github.com/ 注册账号
2. 创建新仓库
3. 将项目代码推送到GitHub

**推送代码到GitHub：**

```bash
# 初始化Git仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/your-username/your-repo.git

# 推送代码
git branch -M main
git push -u origin main
```

### 步骤3：在Vercel导入项目

1. 登录Vercel后，点击 "Add New" → "Project"
2. Vercel会自动显示你的GitHub仓库
3. 点击 "Import" 导入你的仓库

### 步骤4：配置项目设置

#### 根目录配置

如果项目根目录不是根目录，可以设置：
- Root Directory: `/`（默认）

#### 构建配置

Vercel会自动检测Next.js项目，默认配置为：
- Framework Preset: Next.js
- Build Command: `npm run build` 或 `next build`
- Output Directory: `.next`

如果需要自定义，可以手动修改。

#### 环境变量配置（重要！）

在 "Environment Variables" 部分，添加以下变量（从 `.env.local` 复制）：

```env
# 数据库
DATABASE_URL=postgresql://...

# JWT密钥
JWT_SECRET=your-jwt-secret
JWT_REFRESH_SECRET=your-refresh-secret

# 豆包AI
DOUBAO_API_KEY=your-doubao-api-key
DOUBAO_MODEL=doubao-pro-4k

# 邮件服务
EMAIL_HOST=smtp.163.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=your-email@163.com
EMAIL_PASS=your-email-auth-code
EMAIL_FROM=番茄小说AI <your-email@163.com>
EMAIL_MOCK_MODE=false

# 应用配置
NEXT_PUBLIC_BASE_URL=https://your-project.vercel.app
NEXT_PUBLIC_APP_NAME=番茄小说AI写作助手
NODE_ENV=production
```

**重要提示**：
- `NEXT_PUBLIC_BASE_URL` 必须改为Vercel提供的域名
- 部署成功后会显示域名，如 `https://tomato-ai.vercel.app`
- 可以先填一个临时域名，部署成功后再修改

### 步骤5：部署项目

1. 点击 "Deploy" 按钮
2. 等待部署完成（通常需要1-2分钟）
3. 部署成功后会显示：
   - 域名：`https://your-project.vercel.app`
   - 构建日志
   - 预览链接

4. 点击域名访问你的网站

### 步骤6：配置数据库（外部数据库）

Vercel不提供内置数据库，需要使用外部数据库服务。

#### 方案A：使用Railway PostgreSQL（推荐，有免费额度）

1. 访问 https://railway.app/
2. 注册账号（使用GitHub登录）
3. 点击 "New Project" → "Database" → "PostgreSQL"
4. Railway会自动创建PostgreSQL数据库
5. 复制数据库连接字符串

更新Vercel环境变量：
```env
DATABASE_URL=postgresql://postgres:password@containers.railway.app:5432/railway
```

#### 方案B：使用Supabase（推荐，完全免费）

1. 访问 https://supabase.com/
2. 注册账号
3. 创建新项目
4. 复制数据库连接字符串

更新Vercel环境变量：
```env
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-ID].supabase.co:5432/postgres
```

#### 方案C：使用Neon（推荐，免费额度高）

1. 访问 https://neon.tech/
2. 注册账号
3. 创建新项目
4. 复制数据库连接字符串

更新Vercel环境变量：
```env
DATABASE_URL=postgresql://[USER]:[PASSWORD]@[HOST]/[DATABASE]?sslmode=require
```

### 步骤7：运行数据库迁移

**方法1：使用Vercel CLI（推荐）**

1. 安装Vercel CLI：
```bash
npm i -g vercel
```

2. 登录Vercel：
```bash
vercel login
```

3. 在本地执行迁移：
```bash
# 设置环境变量
export DATABASE_URL="your-production-database-url"

# 执行迁移
pnpm run migrate

# 初始化管理员
pnpm run init-admin
```

**方法2：使用Vercel Web Console**

1. 访问Vercel项目页面
2. 点击 "Logs" → "Deployments"
3. 点击 "Console"
4. 执行以下命令：
```bash
pnpm run migrate
pnpm run init-admin
```

**方法3：使用Railway/Supabase的SQL编辑器**

1. 登录Railway/Supabase控制台
2. 打开SQL编辑器
3. 复制 `src/migrations/add_missing_fields.sql` 的内容
4. 粘贴并执行

### 步骤8：验证部署

1. 访问你的域名（如 `https://tomato-ai.vercel.app`）
2. 测试以下功能：
   - [ ] 页面正常加载
   - [ ] 用户注册成功
   - [ ] AI生成功能正常
   - [ ] 文件导入导出正常
3. 检查浏览器控制台是否有错误

---

## 方案二：使用Railway部署

### 优势

✅ 提供免费数据库
✅ 一键部署全栈应用
✅ 支持多种语言
✅ 内置日志和监控

### 步骤1：注册Railway账号

1. 访问 https://railway.app/
2. 点击 "Sign Up"
3. 使用GitHub账号注册

### 步骤2：创建新项目

1. 登录后，点击 "New Project"
2. 选择 "Deploy from GitHub repo"
3. 选择你的GitHub仓库

### 步骤3：配置项目

#### Root Directory
- 保持默认（根目录）

#### Build Command
```
npm run build
```

#### Start Command
```
npm start
```

### 步骤4：添加数据库

1. 在项目页面，点击 "New" → "Database" → "PostgreSQL"
2. Railway会自动创建PostgreSQL数据库
3. 复制数据库连接字符串

### 步骤5：配置环境变量

在项目设置中，添加以下环境变量：

```env
DATABASE_URL=postgresql://postgres:password@containers.railway.app:5432/railway
JWT_SECRET=your-jwt-secret
JWT_REFRESH_SECRET=your-refresh-secret
DOUBAO_API_KEY=your-doubao-api-key
DOUBAO_MODEL=doubao-pro-4k
EMAIL_HOST=smtp.163.com
EMAIL_PORT=465
EMAIL_SECURE=true
EMAIL_USER=your-email@163.com
EMAIL_PASS=your-email-auth-code
EMAIL_FROM=番茄小说AI <your-email@163.com>
EMAIL_MOCK_MODE=false
NEXT_PUBLIC_BASE_URL=https://your-project.railway.app
NEXT_PUBLIC_APP_NAME=番茄小说AI写作助手
NODE_ENV=production
```

### 步骤6：部署项目

1. 点击 "Deploy"
2. 等待部署完成
3. 访问Railway提供的域名

### 步骤7：运行数据库迁移

在Railway Console中执行：

```bash
pnpm run migrate
pnpm run init-admin
```

---

## 方案三：使用Netlify部署

### 优势

✅ 完全免费
✅ 简单易用
✅ 全球CDN
✅ 自动HTTPS

### 步骤1：注册Netlify账号

1. 访问 https://www.netlify.com/
2. 点击 "Sign up"
3. 使用GitHub、GitLab或Bitbucket注册

### 步骤2：导入项目

1. 登录后，点击 "Add new site" → "Import an existing project"
2. 选择GitHub仓库
3. 配置构建设置：
   - Build command: `npm run build`
   - Publish directory: `.next`
   - Functions directory: `netlify/functions`

### 步骤3：配置环境变量

在 "Site settings" → "Environment variables" 中添加：

```env
DATABASE_URL=...
JWT_SECRET=...
JWT_REFRESH_SECRET=...
DOUBAO_API_KEY=...
NEXT_PUBLIC_BASE_URL=...
NODE_ENV=production
```

### 步骤4：部署项目

1. 点击 "Deploy site"
2. 等待部署完成
3. 访问Netlify提供的域名

### 步骤5：配置Next.js适配

Netlify默认不支持Next.js服务端功能，需要配置：

1. 安装依赖：
```bash
pnpm add @netlify/plugin-nextjs
```

2. 创建 `netlify.toml` 文件：
```toml
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
```

3. 重新部署

---

## 方案四：使用国内云服务（阿里云/腾讯云）

### 优势

✅ 国内访问速度快
✅ 数据合规
✅ 支持备案域名

### 阿里云部署步骤

#### 1. 购买服务器

- 选择ECS云服务器
- 推荐配置：2核4G（至少）
- 镜像：Ubuntu 20.04 LTS

#### 2. 配置服务器

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装pnpm
npm install -g pnpm

# 安装Git
sudo apt install -y git

# 安装Nginx
sudo apt install -y nginx

# 安装PM2（进程管理）
npm install -g pm2
```

#### 3. 克隆项目代码

```bash
# 克隆代码
cd /var/www
git clone https://github.com/your-username/your-repo.git
cd your-repo

# 安装依赖
pnpm install

# 配置环境变量
cp .env.production.example .env.local
nano .env.local  # 编辑配置

# 构建项目
pnpm run build

# 运行数据库迁移
pnpm run migrate
pnpm run init-admin
```

#### 4. 使用PM2运行项目

```bash
# 启动项目
pm2 start npm --name "tomato-ai" -- start

# 设置开机自启
pm2 startup
pm2 save

# 查看日志
pm2 logs tomato-ai
```

#### 5. 配置Nginx反向代理

```bash
# 编辑Nginx配置
sudo nano /etc/nginx/sites-available/tomato-ai
```

添加以下内容：

```nginx
server {
    listen 80;
    server_name yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/tomato-ai /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 6. 配置HTTPS（使用Let's Encrypt）

```bash
# 安装Certbot
sudo apt install -y certbot python3-certbot-nginx

# 获取SSL证书
sudo certbot --nginx -d yourdomain.com

# 自动续期
sudo certbot renew --dry-run
```

---

## 绑定自定义域名

### Vercel绑定域名

1. 登录Vercel
2. 进入项目设置
3. 点击 "Domains"
4. 输入你的域名（如 `www.yourdomain.com`）
5. 按照提示配置DNS记录：
   - CNAME记录：`www` → `cname.vercel-dns.com`
6. 等待DNS生效（通常1-24小时）

### Railway绑定域名

1. 登录Railway
2. 进入项目设置
3. 点击 "Networking"
4. 点击 "Add Custom Domain"
5. 输入域名并按照提示配置DNS

### 阿里云绑定域名

1. 登录阿里云控制台
2. 进入"域名解析"
3. 添加A记录：
   - 主机记录：`@` 或 `www`
   - 记录值：服务器IP地址
4. 等待DNS生效

---

## 部署后检查清单

部署完成后，检查以下项目：

### 功能测试

- [ ] 网站可以正常访问
- [ ] 用户可以注册
- [ ] 用户可以登录
- [ ] AI生成功能正常
- [ ] 文件上传功能正常
- [ ] 文件下载功能正常
- [ ] 邮件发送正常（如果配置了）

### 性能测试

- [ ] 页面加载时间 < 3秒
- [ ] AI首字响应时间 < 1秒
- [ ] 数据库查询响应正常
- [ ] 文件上传速度正常

### 安全检查

- [ ] 使用HTTPS
- [ ] 环境变量不泄露
- [ ] API访问有频率限制
- [ ] 数据库连接使用SSL
- [ ] 敏感信息加密存储

### 兼容性测试

- [ ] Chrome浏览器正常
- [ ] Firefox浏览器正常
- [ ] Safari浏览器正常
- [ ] Edge浏览器正常
- [ ] 移动端浏览器正常

---

## 监控和维护

### 日志监控

- **Vercel**: 在项目页面查看 "Logs"
- **Railway**: 在项目页面查看 "Logs"
- **阿里云**: 使用PM2日志 `pm2 logs`

### 性能监控

- 使用Google Analytics分析用户行为
- 使用Sentry监控错误
- 使用Lighthouse测试性能

### 数据备份

- 定期备份数据库
- 备份环境变量配置
- 备份静态资源文件

### 更新维护

- 定期更新依赖包
- 监控安全漏洞
- 及时修复bug
- 根据用户反馈优化功能

---

## 常见部署问题

### Q1：部署后无法连接数据库？

**可能原因**：
- 数据库连接字符串错误
- 数据库未启动
- 防火墙阻止连接

**解决方案**：
- 检查 `DATABASE_URL` 是否正确
- 确认数据库服务正在运行
- 检查防火墙规则

### Q2：AI功能不工作？

**可能原因**：
- `DOUBAO_API_KEY` 未配置或错误
- 账户余额不足
- 网络问题

**解决方案**：
- 检查环境变量配置
- 登录火山引擎检查余额
- 查看错误日志

### Q3：文件上传失败？

**可能原因**：
- 对象存储配置错误
- 文件大小超限
- 权限不足

**解决方案**：
- 检查对象存储配置
- 调整 `MAX_FILE_SIZE` 配置
- 检查S3权限

### Q4：HTTPS配置失败？

**可能原因**：
- DNS未解析
- 域名未备案（国内）
- 证书申请失败

**解决方案**：
- 检查DNS配置
- 完成域名备案
- 重新申请证书

---

## 总结

恭喜！通过本指南，你已经学会了：

✅ 选择合适的部署平台
✅ 完成项目部署
✅ 配置数据库和环境变量
✅ 绑定自定义域名
✅ 进行部署后检查
✅ 监控和维护

你的番茄小说AI写作工具现在已经成功上线了！🎉

---

**文档版本**：v1.0
**最后更新**：2025年
