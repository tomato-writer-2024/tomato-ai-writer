[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
  timeout = 30
  memory = 1024

[dev]
  command = "npm run dev"
  port = 3000
  publish = ".next"
